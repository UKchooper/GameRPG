<UserControl x:Class="GameRPG.View.StartScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameRPG"
        mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Grid.Column="0" Width="300" Height="50" Text="Welcome to this RPG type game. Just having fun trying different coding stuff out!"
                 TextWrapping="Wrap" TextAlignment="Center"/>
        <Button Content="New Game" Grid.Row="1" Grid.Column="0" Width="100" Height="30" Command="{Binding NewGameCommand}"/>
        <Button Content="Load Game" Grid.Row="2" Grid.Column="0" Width="100" Height="30" Click="LoadGame_Click"/>
        <Button Content="Settings" Grid.Row="3" Grid.Column="0" Width="100" Height="30" Click="Settings_Click"/>
    </Grid>
</UserControl>
    
